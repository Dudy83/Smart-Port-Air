{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet">
    {{ encore_entry_link_tags('forms') }}
{% endblock %}

{% block body %}
    
    <div class="form-container w-100 d-flex flex-column justify-content-center align-items-center" style="height:100vh;">
        <form class="d-flex flex-column justify-content-between align-items-stretch" style="color:#fff;" method="post">
            <div class="w-100">
                <h3><i class="far fa-question-circle mr-2"></i>Mot de passe oublié ?</h3>
                <p>Renseignez votre adresse email, et nous vous enverrons les instructions à suivre par mail.</p>
            </div> 
            <div class="group w-100"> 
                <input tabindex="1" id="input-email" type="email" required name="_email">
                <span class="highlight"></span>
                <span class="bar"></span>
                <label for="login-input-email" class="required active"><i class="fas fa-envelope mr-2"></i> Email</label>
                <button tabindex="3" type="submit" class="btn m-0" style="padding: 0; color: #fff; background-color:#6BBA62"><i class="fas fa-sign-in-alt"></i></button>
                <p id="informations">Vous recevrez cet email probablement dans vos fichier de spam, ou débloquez <br> hello@smartport.fr</p>
            </div>
        </form>
    </div>

    <style>
        form
        {
            background: #fff;
            padding: 2.5rem;
            border-radius: 0.25rem;
            width: 25%;
        }

        .group
        {
            margin-bottom: 0 !important;
            position: relative;
        }

        input, .bar
        {
            width: 85%;
        }

        h3
        {
            margin-bottom: 2rem;
            font-weight: bolder;
            color: #363636;
        }

        p
        {
            color: grey;
             margin-bottom: 2rem;
        }

        #informations
        {
            font-size: 60%;
            margin-bottom: 0;
            text-align: center;
        }

        button[type="submit"]
        {
            margin-left: 1rem;
            width: 48px;
            height: 47px;
            position: absolute;
            top: 0px;
            right: 0;
        }

        label
        {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .bar
        {
               margin-bottom: 45px;
        }

        @media(max-width: 1500px)
        {
            form
            {
                background-color: transparent;
                width: 90% !important;
            }

            h3, p
            {
                color: #fff;
            }
        }
    </style>


{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('forms') }}
{% endblock %}